import 'package:flutter/material.dart';

class ArogyamScreen extends StatelessWidget {
  const ArogyamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SafeArea(child: Center(child: Text('Arogyam'))));
  }
}
